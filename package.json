{
  "name": "@personal/source",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nx run web:dev",
    "build": "nx run web:build",
    "start": "nx run web:start"
  },
  "devDependencies": {
    "nx": "^20.3.0",
    "typescript": "^5.7.0"
  }
}
